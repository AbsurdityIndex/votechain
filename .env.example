# ──────────────────────────────────────────────────────────────
# VoteChain — Environment Variables
# ──────────────────────────────────────────────────────────────
# Copy to .env and fill in values.
# Secrets (marked 🔒) should ALSO be set via `wrangler pages secret put`
# or the Cloudflare dashboard for production — never commit real values.
# ──────────────────────────────────────────────────────────────

# ── Cloudflare Pages Functions (runtime) ─────────────────────
# These are read by functions/api/votechain/poc/*.js at request time.

TURNSTILE_SECRET_KEY=           # Cloudflare Turnstile secret key
POC_ACCESS_COOKIE_SECRET=       # HMAC key for POC session cookies

# Worker node URLs (where Pages Functions proxy replication requests)
VOTECHAIN_FEDERAL_NODE_URL=     # e.g. https://votechain-federal-node.corey-steinwand.workers.dev
VOTECHAIN_STATE_NODE_URL=       # e.g. https://votechain-state-node.corey-steinwand.workers.dev
VOTECHAIN_OVERSIGHT_NODE_URL=   # e.g. https://votechain-oversight-node.corey-steinwand.workers.dev

# Write tokens for authenticated replication to Workers
VOTECHAIN_FEDERAL_WRITE_TOKEN=  # Bearer token for federal node
VOTECHAIN_STATE_WRITE_TOKEN=    # Bearer token for state node
VOTECHAIN_OVERSIGHT_WRITE_TOKEN= # Bearer token for oversight node

# ── Build-time (PUBLIC_ prefix → available via import.meta.env) ──
# Used by Astro/Vite during `npm run build`. The POC client reads
# these to know where Workers live.

PUBLIC_TURNSTILE_SITE_KEY=      # Cloudflare Turnstile site key (public)
PUBLIC_VOTECHAIN_WORKERS_BASE=  # e.g. https://votechain-{role}-node.corey-steinwand.workers.dev

# Per-node overrides (optional — PUBLIC_VOTECHAIN_WORKERS_BASE covers all three)
# PUBLIC_VOTECHAIN_FEDERAL_NODE_URL=
# PUBLIC_VOTECHAIN_STATE_NODE_URL=
# PUBLIC_VOTECHAIN_OVERSIGHT_NODE_URL=

# ── CI / CD ──────────────────────────────────────────────────
# Used by wrangler CLI for deployments. Set in CI secrets, not here.

# CLOUDFLARE_API_TOKEN=         # API token with Pages + Workers permissions
# CLOUDFLARE_ACCOUNT_ID=        # Cloudflare account ID
